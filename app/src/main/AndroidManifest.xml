<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finddine"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-feature android:name="android.hardware.wifi.rtt" /> <!-- Permissions required for Wifi RTT. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomAppTheme">
        <activity
            android:name=".ClientInterface.HomeScreen"
            android:label="@string/home_screen"
            android:theme="@style/CustomAppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DevMenu.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/CustomAppTheme">
        </activity>
        <activity
            android:name=".DevMenu.LocationPermissionRequestActivity"
            android:label="@string/title_activity_location_permission_request"
            android:parentActivityName=".DevMenu.MainActivity"
            android:theme="@style/CustomAppTheme"></activity>
        <activity
            android:name=".DevMenu.AccessPointRangingResultsActivity"
            android:label="@string/title_activity_access_point_ranging_results"
            android:parentActivityName=".DevMenu.MainActivity"
            android:theme="@style/CustomAppTheme"></activity>
    </application>

</manifest>